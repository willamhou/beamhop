import Foundation
import BeamhopCore

// Placeholder entry point — the AppKit menu-bar shell (MenuBarController, HotkeyManager,
// PermissionService, Diagnostics) is added next. Kept minimal so the package builds while we
// validate BeamhopCore (storage) first.
print("Beamhop \(Version.beamhop) — foundation build OK")
