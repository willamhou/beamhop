{
  "name": "beamhop-extension-build",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild readability-entry.js --bundle --format=iife --outfile=../vendor/readability.js"
  },
  "devDependencies": {
    "@mozilla/readability": "^0.5.0",
    "esbuild": "^0.23.0"
  }
}
